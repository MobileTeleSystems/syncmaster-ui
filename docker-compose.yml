version: "3.8"
services:
    syncmaster-ui:
        image: mtsrus/syncmaster-ui
        build:
            dockerfile: Dockerfile
            context: .
            network: host
            target: prod
            args:
                - API_URL=localhost:8000

        ports:
            - 3000:3000
