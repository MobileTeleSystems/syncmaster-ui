import { Alert } from "@mui/material";

const Loading= () => {
    return (
        <Alert severity="info" style={{ paddingTop: "1em" }}>
            Loading...
        </Alert>
    );
};

export default Loading;
